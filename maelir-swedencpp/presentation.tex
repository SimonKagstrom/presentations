\documentclass[aspectratio=169]{beamer}


\usepackage[utf8]{inputenc}
\usepackage{pgfpages}
\usepackage{xcolor}

\setbeameroption{show notes}
\setbeamertemplate{note page}[plain]
\setbeameroption{show notes on second screen=left}
%\setbeameroption{show only notes}

\usetheme{Warsaw}
\graphicspath{ {./images/}{../images/}{../kcov-swedencpp/images/}{../../kcov-swedencpp/images/}{output/} }

\title[Floats, literally]
{Floats, literally}

%\subtitle{A short story}

\author{Simon Kågström}

\institute
{
  Consultant\\
  \texttt{https://github.com/SimonKagstrom/libmaelir} \\
  \texttt{https://github.com/SimonKagstrom/maelir} \\
  \texttt{https://github.com/SimonKagstrom/radbuzz}
}

% I bought a boat. Old norwegian fiberglass boat called Selco 13B. It's from the 1960s.

% Not the two-stroke meetup, but the four-stroke meetup (C++)

% It's in Mälaren, which is shaped like a maze (show the three largest), so I thought i'd need a gps

% Three kinds: Ugly, expensive and ugly and expensive. Since I'm a software developer, I thought I'd just make my own instead


% I wanted something that would look like it came with the boat as original equipment in 1964 and that would answer three questions:
% How fast am I going? Where am I? How do I get home?
% This is the end result


% This would have been the end of the introduction, but I also turned 50 this year. You all know what that means. I'm supposed to
% buy a motorcycle. Howver, I value my life, so I got a moped instead.


% About esp32
% Many kinds (c3, c6, s3, p4 etc)
% Programmed via the esp-idf, all look the same.
% Not even the same CPU architecture (xtensa vs risc-v)

% esp-idf
% Setup (eim install, cmake, how I use standard cmake)
% Some example APIs
% Components

% About etl. Examples for e.g.,
% etl::vector (std::inplace_vector), etl::queue_spsc_atomic for interrupt/thread communication, etl::bitset, which has fast find-first-bit
% etl::mutex, etl::circular_buffer, etl::unordered_map etc

% Using the simulator plus unit tests
